// pages/search/index.js

import regeneratorRuntime from '../../lib/runtime/runtime';
import { request } from '../../request/index.js'


Page({

  /**
   * 页面的初始数据
   */
  data: {

  },

  // 绑定输入框事件
  handleInput(e){
     console.log(e);
     const {value} = e.dedail
     
  }


})